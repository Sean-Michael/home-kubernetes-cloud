apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: comfyui
  namespace: comfyui
spec:
  parentRefs:
    - name: caliban-gateway
      namespace: istio-ingress
  hostnames:
    - comfyui.tail4dd976.ts.net
  rules:
    - backendRefs:
        - name: comfyui
          port: 8188
